<div class="page-container d-flex align-items-center justify-content-center">
  <div class="half-width-container w grey-bg ps2">
    <% @objects.each_with_index do |contact, index| %>
    <%= render "contacts/contact", contact: contact, index: index %>
    <% end %>
  </div>
</div>
